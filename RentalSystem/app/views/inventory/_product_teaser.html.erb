

<div class = "grid">
    <h3><%= product.title %></h3>
    <p><%= product.description %></p>
    <p><%= product.price %></p>
    <% if product.available %>
        <p>Available for Rent</p>
    <% else %>
        <p>Not Available</p>
    <% end %>
</div>